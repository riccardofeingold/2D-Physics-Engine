#ifndef COLORS_HPP
#define COLORS_HPP

#include <SFML/Graphics.hpp>

struct Colors
{
    sf::Color PLAYER = sf::Color(12, 53, 106);
    sf::Color GOAL = sf::Color(0, 233, 162);
    sf::Color ENEMY = sf::Color(252, 41, 71);
    sf::Color BACKGROUND = sf::Color(39, 158, 255);   
};

#endif